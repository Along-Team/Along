import 'package:flutter/material.dart';

class TripsTransactionPage extends StatelessWidget {
  const TripsTransactionPage({super.key});
  static const routhName = 'trips-transaction-page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
      body: Center(
        child: Text("Trips Transaction Page"),
      ),
    );
  }
}
