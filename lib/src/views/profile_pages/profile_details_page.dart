import 'package:flutter/material.dart';

class ProfileDetailsPage extends StatelessWidget {
  const ProfileDetailsPage({super.key});
  static const routhName = "profiles-details-page";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}