import 'package:flutter/material.dart';

class AddressSettingsPage extends StatelessWidget {
  const AddressSettingsPage({super.key});
  static const routhName = "address-settings-page";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
